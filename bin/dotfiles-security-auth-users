#!/bin/python3

import os

def read_passwd(): 
    userlist = []
    with open("/etc/passwd", "r") as passwd:
        for line in passwd:
            userlist.append(line.split(":")[0])

    return userlist

def prompt_auth_users():
    userlist = []
    while(True):
        inp = input("Enter authorized user, or \"break\" to exit: ")
        if inp == "break":
           break
        authuserlist.append(inp)
    return userlist

# get sudo access
def prompt_sudo():
    ret = 0
    if os.geteuid() != 0:
        msg = "[sudo] password for %u:"
        ret = subprocess.check_call("sudo -v -p '%s'" % msg, shell=True)
    return ret

def users_to_delete(

authuserlist = prompt_auth_users()
actualuserlist = read_passwd()






    
    
# then delete those users who are not on the list

# then get admin users

# then deadmin users who are not on the list


